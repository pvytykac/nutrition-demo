schema: intent-driven
rules:
  proposal:
    - Must always use grill-me skill
  design:
    - Must always use c4-diagram skill
  adr:
    - Must always use architectural-decision-records skill
defaultSchema: intent-driven
